---
// =============================================================================
//  Panel de edición
//  Es la ÚNICA ruta del sitio público que se ejecuta en el servidor.
//  Todo lo demás es HTML generado al compilar y servido desde el borde de la red.
// =============================================================================
export const prerender = false;

import { makePage } from '@keystatic/astro/ui';
import config from '../../../keystatic.config';

const Page = makePage(config);
---

<Page />
